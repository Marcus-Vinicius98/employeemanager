spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employeemanager
    name: root  
    password: banco123
   
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
     dialect: org.hibernate.dialect.MySQL8Dialect