spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employeemanager
    name: root  
    password: banco123
   
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
     dialect: org.hibernate.dialect.MySQL8Dialect